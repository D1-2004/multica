// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: task_completion.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const claimTaskCompletion = `-- name: ClaimTaskCompletion :one
WITH candidate AS (
    SELECT id
    FROM task_completion_outbox queued
    WHERE queued.status = 'queued'
      AND queued.target_identity = $1
      AND queued.available_at <= now()
      AND (queued.lease_expires_at IS NULL OR queued.lease_expires_at <= now())
    ORDER BY queued.available_at, queued.created_at
    FOR UPDATE SKIP LOCKED
    LIMIT 1
)
UPDATE task_completion_outbox AS completion
SET lease_token = gen_random_uuid(),
    lease_expires_at = now() + interval '2 minutes',
    updated_at = now()
FROM candidate
WHERE completion.id = candidate.id
RETURNING completion.id, completion.root_task_id, completion.terminal_task_id, completion.callback_url, completion.target_identity, completion.request_id, completion.agent_id, completion.external_session_id, completion.execution_status, completion.result_message, completion.error, completion.failure_reason, completion.status, completion.available_at, completion.attempt_count, completion.lease_token, completion.lease_expires_at, completion.last_error, completion.delivered_at, completion.created_at, completion.updated_at
`

func (q *Queries) ClaimTaskCompletion(ctx context.Context, workerTargetIdentity string) (TaskCompletionOutbox, error) {
	row := q.db.QueryRow(ctx, claimTaskCompletion, workerTargetIdentity)
	var i TaskCompletionOutbox
	err := row.Scan(
		&i.ID,
		&i.RootTaskID,
		&i.TerminalTaskID,
		&i.CallbackUrl,
		&i.TargetIdentity,
		&i.RequestID,
		&i.AgentID,
		&i.ExternalSessionID,
		&i.ExecutionStatus,
		&i.ResultMessage,
		&i.Error,
		&i.FailureReason,
		&i.Status,
		&i.AvailableAt,
		&i.AttemptCount,
		&i.LeaseToken,
		&i.LeaseExpiresAt,
		&i.LastError,
		&i.DeliveredAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const completeTaskCompletion = `-- name: CompleteTaskCompletion :one
UPDATE task_completion_outbox
SET status = 'delivered',
    attempt_count = attempt_count + 1,
    lease_token = NULL,
    lease_expires_at = NULL,
    delivered_at = now(),
    updated_at = now()
WHERE id = $1
  AND lease_token = $2
  AND status = 'queued'
RETURNING id, root_task_id, terminal_task_id, callback_url, target_identity, request_id, agent_id, external_session_id, execution_status, result_message, error, failure_reason, status, available_at, attempt_count, lease_token, lease_expires_at, last_error, delivered_at, created_at, updated_at
`

type CompleteTaskCompletionParams struct {
	ID         pgtype.UUID `json:"id"`
	LeaseToken pgtype.UUID `json:"lease_token"`
}

func (q *Queries) CompleteTaskCompletion(ctx context.Context, arg CompleteTaskCompletionParams) (TaskCompletionOutbox, error) {
	row := q.db.QueryRow(ctx, completeTaskCompletion, arg.ID, arg.LeaseToken)
	var i TaskCompletionOutbox
	err := row.Scan(
		&i.ID,
		&i.RootTaskID,
		&i.TerminalTaskID,
		&i.CallbackUrl,
		&i.TargetIdentity,
		&i.RequestID,
		&i.AgentID,
		&i.ExternalSessionID,
		&i.ExecutionStatus,
		&i.ResultMessage,
		&i.Error,
		&i.FailureReason,
		&i.Status,
		&i.AvailableAt,
		&i.AttemptCount,
		&i.LeaseToken,
		&i.LeaseExpiresAt,
		&i.LastError,
		&i.DeliveredAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deadLetterTaskCompletion = `-- name: DeadLetterTaskCompletion :one
UPDATE task_completion_outbox
SET status = 'dead_letter',
    attempt_count = attempt_count + 1,
    lease_token = NULL,
    lease_expires_at = NULL,
    last_error = $3,
    updated_at = now()
WHERE id = $1
  AND lease_token = $2
  AND status = 'queued'
RETURNING id, root_task_id, terminal_task_id, callback_url, target_identity, request_id, agent_id, external_session_id, execution_status, result_message, error, failure_reason, status, available_at, attempt_count, lease_token, lease_expires_at, last_error, delivered_at, created_at, updated_at
`

type DeadLetterTaskCompletionParams struct {
	ID         pgtype.UUID `json:"id"`
	LeaseToken pgtype.UUID `json:"lease_token"`
	LastError  pgtype.Text `json:"last_error"`
}

func (q *Queries) DeadLetterTaskCompletion(ctx context.Context, arg DeadLetterTaskCompletionParams) (TaskCompletionOutbox, error) {
	row := q.db.QueryRow(ctx, deadLetterTaskCompletion, arg.ID, arg.LeaseToken, arg.LastError)
	var i TaskCompletionOutbox
	err := row.Scan(
		&i.ID,
		&i.RootTaskID,
		&i.TerminalTaskID,
		&i.CallbackUrl,
		&i.TargetIdentity,
		&i.RequestID,
		&i.AgentID,
		&i.ExternalSessionID,
		&i.ExecutionStatus,
		&i.ResultMessage,
		&i.Error,
		&i.FailureReason,
		&i.Status,
		&i.AvailableAt,
		&i.AttemptCount,
		&i.LeaseToken,
		&i.LeaseExpiresAt,
		&i.LastError,
		&i.DeliveredAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const enqueueSynchronousTaskCompletion = `-- name: EnqueueSynchronousTaskCompletion :one
INSERT INTO task_completion_outbox AS existing (
    root_task_id,
    terminal_task_id,
    callback_url,
    target_identity,
    request_id,
    agent_id,
    execution_status,
    result_message,
    error,
    failure_reason
) VALUES (
    NULL, NULL, $1, $2, $3, $4, 'failed', '', $5, $6
)
ON CONFLICT (request_id) DO UPDATE
SET updated_at = existing.updated_at
WHERE existing.root_task_id IS NULL
  AND existing.terminal_task_id IS NULL
  AND existing.callback_url = EXCLUDED.callback_url
  AND existing.target_identity = EXCLUDED.target_identity
  AND existing.agent_id = EXCLUDED.agent_id
  AND existing.execution_status = EXCLUDED.execution_status
  AND existing.result_message = EXCLUDED.result_message
  AND existing.error IS NOT DISTINCT FROM EXCLUDED.error
  AND existing.failure_reason IS NOT DISTINCT FROM EXCLUDED.failure_reason
RETURNING id, root_task_id, terminal_task_id, callback_url, target_identity, request_id, agent_id, external_session_id, execution_status, result_message, error, failure_reason, status, available_at, attempt_count, lease_token, lease_expires_at, last_error, delivered_at, created_at, updated_at
`

type EnqueueSynchronousTaskCompletionParams struct {
	CallbackUrl    string      `json:"callback_url"`
	TargetIdentity string      `json:"target_identity"`
	RequestID      string      `json:"request_id"`
	AgentID        pgtype.UUID `json:"agent_id"`
	Error          pgtype.Text `json:"error"`
	FailureReason  pgtype.Text `json:"failure_reason"`
}

func (q *Queries) EnqueueSynchronousTaskCompletion(ctx context.Context, arg EnqueueSynchronousTaskCompletionParams) (TaskCompletionOutbox, error) {
	row := q.db.QueryRow(ctx, enqueueSynchronousTaskCompletion,
		arg.CallbackUrl,
		arg.TargetIdentity,
		arg.RequestID,
		arg.AgentID,
		arg.Error,
		arg.FailureReason,
	)
	var i TaskCompletionOutbox
	err := row.Scan(
		&i.ID,
		&i.RootTaskID,
		&i.TerminalTaskID,
		&i.CallbackUrl,
		&i.TargetIdentity,
		&i.RequestID,
		&i.AgentID,
		&i.ExternalSessionID,
		&i.ExecutionStatus,
		&i.ResultMessage,
		&i.Error,
		&i.FailureReason,
		&i.Status,
		&i.AvailableAt,
		&i.AttemptCount,
		&i.LeaseToken,
		&i.LeaseExpiresAt,
		&i.LastError,
		&i.DeliveredAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const enqueueTaskCompletion = `-- name: EnqueueTaskCompletion :one
INSERT INTO task_completion_outbox AS existing (
    root_task_id,
    terminal_task_id,
    callback_url,
    target_identity,
    request_id,
    agent_id,
    external_session_id,
    execution_status,
    result_message,
    error,
    failure_reason
) VALUES (
    $1, $2, $3, $4, $5, $6,
    $9,
    $7, $8,
    $10,
    $11
)
ON CONFLICT (root_task_id) DO UPDATE
SET updated_at = existing.updated_at
WHERE existing.terminal_task_id = EXCLUDED.terminal_task_id
  AND existing.callback_url = EXCLUDED.callback_url
  AND existing.target_identity = EXCLUDED.target_identity
  AND existing.request_id = EXCLUDED.request_id
  AND existing.agent_id = EXCLUDED.agent_id
  AND existing.external_session_id IS NOT DISTINCT FROM EXCLUDED.external_session_id
  AND existing.execution_status = EXCLUDED.execution_status
  AND existing.result_message = EXCLUDED.result_message
  AND existing.error IS NOT DISTINCT FROM EXCLUDED.error
  AND existing.failure_reason IS NOT DISTINCT FROM EXCLUDED.failure_reason
RETURNING id, root_task_id, terminal_task_id, callback_url, target_identity, request_id, agent_id, external_session_id, execution_status, result_message, error, failure_reason, status, available_at, attempt_count, lease_token, lease_expires_at, last_error, delivered_at, created_at, updated_at
`

type EnqueueTaskCompletionParams struct {
	RootTaskID        pgtype.UUID `json:"root_task_id"`
	TerminalTaskID    pgtype.UUID `json:"terminal_task_id"`
	CallbackUrl       string      `json:"callback_url"`
	TargetIdentity    string      `json:"target_identity"`
	RequestID         string      `json:"request_id"`
	AgentID           pgtype.UUID `json:"agent_id"`
	ExecutionStatus   string      `json:"execution_status"`
	ResultMessage     string      `json:"result_message"`
	ExternalSessionID pgtype.Text `json:"external_session_id"`
	Error             pgtype.Text `json:"error"`
	FailureReason     pgtype.Text `json:"failure_reason"`
}

func (q *Queries) EnqueueTaskCompletion(ctx context.Context, arg EnqueueTaskCompletionParams) (TaskCompletionOutbox, error) {
	row := q.db.QueryRow(ctx, enqueueTaskCompletion,
		arg.RootTaskID,
		arg.TerminalTaskID,
		arg.CallbackUrl,
		arg.TargetIdentity,
		arg.RequestID,
		arg.AgentID,
		arg.ExecutionStatus,
		arg.ResultMessage,
		arg.ExternalSessionID,
		arg.Error,
		arg.FailureReason,
	)
	var i TaskCompletionOutbox
	err := row.Scan(
		&i.ID,
		&i.RootTaskID,
		&i.TerminalTaskID,
		&i.CallbackUrl,
		&i.TargetIdentity,
		&i.RequestID,
		&i.AgentID,
		&i.ExternalSessionID,
		&i.ExecutionStatus,
		&i.ResultMessage,
		&i.Error,
		&i.FailureReason,
		&i.Status,
		&i.AvailableAt,
		&i.AttemptCount,
		&i.LeaseToken,
		&i.LeaseExpiresAt,
		&i.LastError,
		&i.DeliveredAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getLastTaskReplyText = `-- name: GetLastTaskReplyText :one
WITH RECURSIVE lineage AS (
    SELECT task.id, task.parent_task_id
    FROM agent_task_queue task
    WHERE task.id = $1

    UNION ALL

    SELECT parent.id, parent.parent_task_id
    FROM agent_task_queue parent
    JOIN lineage child ON parent.id = child.parent_task_id
)
SELECT reply.content
FROM (
    SELECT message.content, message.created_at
    FROM task_message message
    JOIN lineage ON lineage.id = message.task_id
    WHERE message.type = 'text'
      AND COALESCE(BTRIM(message.content), '') <> ''

    UNION ALL

    SELECT task_comment.content, task_comment.created_at
    FROM comment task_comment
    JOIN lineage ON lineage.id = task_comment.source_task_id
    WHERE task_comment.author_type = 'agent'
      AND COALESCE(BTRIM(task_comment.content), '') <> ''
) AS reply
ORDER BY reply.created_at DESC
LIMIT 1
`

func (q *Queries) GetLastTaskReplyText(ctx context.Context, id pgtype.UUID) (pgtype.Text, error) {
	row := q.db.QueryRow(ctx, getLastTaskReplyText, id)
	var content pgtype.Text
	err := row.Scan(&content)
	return content, err
}

const getTaskCompletionByRequestID = `-- name: GetTaskCompletionByRequestID :one
SELECT id, root_task_id, terminal_task_id, callback_url, target_identity, request_id, agent_id, external_session_id, execution_status, result_message, error, failure_reason, status, available_at, attempt_count, lease_token, lease_expires_at, last_error, delivered_at, created_at, updated_at
FROM task_completion_outbox
WHERE request_id = $1
`

func (q *Queries) GetTaskCompletionByRequestID(ctx context.Context, requestID string) (TaskCompletionOutbox, error) {
	row := q.db.QueryRow(ctx, getTaskCompletionByRequestID, requestID)
	var i TaskCompletionOutbox
	err := row.Scan(
		&i.ID,
		&i.RootTaskID,
		&i.TerminalTaskID,
		&i.CallbackUrl,
		&i.TargetIdentity,
		&i.RequestID,
		&i.AgentID,
		&i.ExternalSessionID,
		&i.ExecutionStatus,
		&i.ResultMessage,
		&i.Error,
		&i.FailureReason,
		&i.Status,
		&i.AvailableAt,
		&i.AttemptCount,
		&i.LeaseToken,
		&i.LeaseExpiresAt,
		&i.LastError,
		&i.DeliveredAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getTaskCompletionTarget = `-- name: GetTaskCompletionTarget :one

WITH RECURSIVE lineage AS (
    SELECT task.id, task.parent_task_id, task.context, 0 AS depth
    FROM agent_task_queue task
    WHERE task.id = $1

    UNION ALL

    SELECT parent.id, parent.parent_task_id, parent.context, child.depth + 1
    FROM agent_task_queue parent
    JOIN lineage child ON parent.id = child.parent_task_id
)
SELECT
    lineage.id AS root_task_id,
    COALESCE(lineage.context #>> '{completion_callback,url}', '')::text AS callback_url,
    COALESCE(lineage.context #>> '{completion_callback,target}', '')::text AS target_identity
FROM lineage
WHERE lineage.parent_task_id IS NULL
  AND COALESCE(lineage.context #>> '{completion_callback,url}', '') <> ''
  AND COALESCE(lineage.context #>> '{completion_callback,target}', '') <> ''
ORDER BY lineage.depth DESC
LIMIT 1
`

type GetTaskCompletionTargetRow struct {
	RootTaskID     pgtype.UUID `json:"root_task_id"`
	CallbackUrl    string      `json:"callback_url"`
	TargetIdentity string      `json:"target_identity"`
}

// =====================
// Task completion outbox
// =====================
func (q *Queries) GetTaskCompletionTarget(ctx context.Context, id pgtype.UUID) (GetTaskCompletionTargetRow, error) {
	row := q.db.QueryRow(ctx, getTaskCompletionTarget, id)
	var i GetTaskCompletionTargetRow
	err := row.Scan(&i.RootTaskID, &i.CallbackUrl, &i.TargetIdentity)
	return i, err
}

const listTerminalTaskIDsMissingCompletion = `-- name: ListTerminalTaskIDsMissingCompletion :many
WITH RECURSIVE lineage AS (
    SELECT
        terminal.id AS terminal_task_id,
        terminal.id AS current_task_id,
        terminal.parent_task_id,
        terminal.context,
        COALESCE(terminal.context #>> '{completion_callback,target}', '')::text AS target_identity,
        terminal.completed_at,
        terminal.created_at
    FROM agent_task_queue terminal
    WHERE terminal.status IN ('completed', 'failed', 'cancelled')
      AND COALESCE(terminal.context #>> '{completion_callback,url}', '') <> ''
      AND jsonb_extract_path_text(
          terminal.context,
          'completion_callback',
          'target'
      ) = $2::text
      AND NOT EXISTS (
          SELECT 1
          FROM agent_task_queue child
          WHERE child.parent_task_id = terminal.id
      )

    UNION ALL

    SELECT
        child.terminal_task_id,
        parent.id,
        parent.parent_task_id,
        parent.context,
        COALESCE(parent.context #>> '{completion_callback,target}', '')::text,
        child.completed_at,
        child.created_at
    FROM lineage child
    JOIN agent_task_queue parent ON parent.id = child.parent_task_id
)
SELECT root.terminal_task_id
FROM lineage root
WHERE root.parent_task_id IS NULL
  AND COALESCE(root.context #>> '{completion_callback,url}', '') <> ''
  AND NOT EXISTS (
      SELECT 1
      FROM task_completion_outbox completion
      WHERE completion.root_task_id = root.current_task_id
  )
ORDER BY root.completed_at, root.created_at
LIMIT $1
`

type ListTerminalTaskIDsMissingCompletionParams struct {
	BatchLimit           int32  `json:"batch_limit"`
	WorkerTargetIdentity string `json:"worker_target_identity"`
}

func (q *Queries) ListTerminalTaskIDsMissingCompletion(ctx context.Context, arg ListTerminalTaskIDsMissingCompletionParams) ([]pgtype.UUID, error) {
	rows, err := q.db.Query(ctx, listTerminalTaskIDsMissingCompletion, arg.BatchLimit, arg.WorkerTargetIdentity)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []pgtype.UUID{}
	for rows.Next() {
		var terminal_task_id pgtype.UUID
		if err := rows.Scan(&terminal_task_id); err != nil {
			return nil, err
		}
		items = append(items, terminal_task_id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const retryTaskCompletion = `-- name: RetryTaskCompletion :one
UPDATE task_completion_outbox
SET available_at = $3,
    attempt_count = attempt_count + 1,
    lease_token = NULL,
    lease_expires_at = NULL,
    last_error = $4,
    updated_at = now()
WHERE id = $1
  AND lease_token = $2
  AND status = 'queued'
RETURNING id, root_task_id, terminal_task_id, callback_url, target_identity, request_id, agent_id, external_session_id, execution_status, result_message, error, failure_reason, status, available_at, attempt_count, lease_token, lease_expires_at, last_error, delivered_at, created_at, updated_at
`

type RetryTaskCompletionParams struct {
	ID          pgtype.UUID        `json:"id"`
	LeaseToken  pgtype.UUID        `json:"lease_token"`
	AvailableAt pgtype.Timestamptz `json:"available_at"`
	LastError   pgtype.Text        `json:"last_error"`
}

func (q *Queries) RetryTaskCompletion(ctx context.Context, arg RetryTaskCompletionParams) (TaskCompletionOutbox, error) {
	row := q.db.QueryRow(ctx, retryTaskCompletion,
		arg.ID,
		arg.LeaseToken,
		arg.AvailableAt,
		arg.LastError,
	)
	var i TaskCompletionOutbox
	err := row.Scan(
		&i.ID,
		&i.RootTaskID,
		&i.TerminalTaskID,
		&i.CallbackUrl,
		&i.TargetIdentity,
		&i.RequestID,
		&i.AgentID,
		&i.ExternalSessionID,
		&i.ExecutionStatus,
		&i.ResultMessage,
		&i.Error,
		&i.FailureReason,
		&i.Status,
		&i.AvailableAt,
		&i.AttemptCount,
		&i.LeaseToken,
		&i.LeaseExpiresAt,
		&i.LastError,
		&i.DeliveredAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
